{
  "server": {
    "port": 10800
  },
  "mountings": {
    "/": {
      "mongodb": {
        "connectionUri": "mongodb://${AUTHPARAMS}${REALTIMEDB}:${REALTIMEDBPORT}/${AUTHDB}?sockettimeoutms=${QUERYTIMEOUT}"
      }
    }
  }
}
